import chartjs from 'chart-js-luxon-reproduction/utils/chartjs';
import { module, test } from 'qunit';

module('Unit | Utility | chartjs', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = chartjs();
    assert.ok(result);
  });
});
